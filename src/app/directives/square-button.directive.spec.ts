import { SquareGrayButtonDirective } from './square-button.directive';

describe('SquareGrayButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SquareGrayButtonDirective();
    expect(directive).toBeTruthy();
  });
});
